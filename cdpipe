pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/janathiru0101-lgtm/janacdpipeline.git'
            }
        }
        stage('Build') {
            steps {
                echo 'Building Application...'
                // Use 'bat' for Windows, and 'dir' instead of 'ls'
                bat 'dir' 
            }
        }
        stage('Test') {
            steps {
                echo 'Running Tests...'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to Cloud...'
                echo 'Deployment Successful!'
            }
        }
    }
}
