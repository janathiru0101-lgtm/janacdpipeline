pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                // Ensure this URL is your actual project URL
                git branch: 'main', url: 'https://github.com/your-username/your-repo.git'
                echo 'Source code pulled successfully.'
            }
        }

        stage('Build') {
            steps {
                echo 'Building Application...'
                // Just listing files to prove the build environment is ready
                sh 'ls -ltr' 
            }
        }

        stage('Test') {
            steps {
                echo 'Testing Application...'
                echo 'Artifact verification passed.'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying to Cloud...'
                // If you are not using a plugin, this is the standard way to deploy:
                // sh 'scp index.html user@your-cloud-ip:/var/www/html/'
                
                echo 'Deployment status: SUCCESS'
            }
        }
    }
}
